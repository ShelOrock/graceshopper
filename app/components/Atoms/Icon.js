import * as React from 'react';

import * as StyledComponents from '../StyledComponents';
const { StyledImage: { StyledIcon } } = StyledComponents;

export default ({ src }) => <StyledIcon src={ src } />;
