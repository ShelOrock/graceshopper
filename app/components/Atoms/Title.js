import * as React from 'react';

import * as StyledComponents from '../StyledComponents';
const { StyledType: { StyledTitle } } = StyledComponents

export default ({ children }) => <StyledTitle>{ children }</StyledTitle>;
