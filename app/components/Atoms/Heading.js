import * as React from 'react';

import * as StyledComponents from '../StyledComponents';
const { StyledType: { StyledHeading } } = StyledComponents

export default ({ children }) => <StyledHeading>{ children }</StyledHeading>;
