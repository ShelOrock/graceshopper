import * as React from 'react';

import * as StyledComponents from '../StyledComponents';
const { StyledImage: { StyledHeaderImage } } = StyledComponents;

export default ({ src }) => <StyledHeaderImage src={ src } />;
