import React from 'react';

import { StyledType } from '../../StyledComponents';

const SubHeading = ({ children }) => <StyledType.SubHeading>{ children }</StyledType.SubHeading>;

export default SubHeading;
