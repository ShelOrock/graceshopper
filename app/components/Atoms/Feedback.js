import * as React from 'react';

import * as StyledComponents from '../StyledComponents';
const { StyledForm: { StyledFeedback } } = StyledComponents;

export default ({ children }) => <StyledFeedback>{ children }</StyledFeedback>;
