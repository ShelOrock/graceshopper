import * as React from 'react';

import * as StyledComponents from '../StyledComponents';
const { StyledStripe: { StyledSecurityCode } } = StyledComponents;

export default () => <StyledSecurityCode />;
