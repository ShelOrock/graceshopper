import * as React from 'react';

import * as StyledComponents from '../StyledComponents';
const { StyledProductPage: { StyledImage } } = StyledComponents;

export default ({ src }) => <StyledImage src={ src } />;
