import * as React from 'react';

import * as StyledComponents from '../StyledComponents';
const { StyledType: { StyledSubHeading } } = StyledComponents

export default ({ children }) => <StyledSubHeading>{ children }</StyledSubHeading>;
