import * as React from 'react';

import * as StyledComponents from '../StyledComponents';
const { StyledType: { StyledSubTitle } } = StyledComponents

export default ({ children }) => <StyledSubTitle>{ children }</StyledSubTitle>;
