import * as React from 'react';

import * as StyledComponents from '../StyledComponents';
const { StyledType: { StyledBody } } = StyledComponents

export default ({ children }) => <StyledBody>{ children }</StyledBody>;
