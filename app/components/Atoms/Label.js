import * as React from 'react';

import * as StyledComponents from '../StyledComponents';
const { StyledForm: { StyledLabel } } = StyledComponents;

export default ({ children }) => <StyledLabel>{ children }</StyledLabel>;
