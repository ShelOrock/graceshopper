import * as React from 'react';

import * as StyledComponents from '../StyledComponents';
const { StyledList: { StyledLineList } } = StyledComponents

export default ({ children }) => <StyledLineList>{ children }</StyledLineList>;
