import * as React from 'react';

import * as StyledComponents from '../StyledComponents';
const { StyledType: { StyledSmallBody } } = StyledComponents

export default ({ children }) => <StyledSmallBody>{ children }</StyledSmallBody>;
