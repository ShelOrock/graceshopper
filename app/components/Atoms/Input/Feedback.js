import  React from 'react';

import { StyledInput } from '../../StyledComponents';

const Feedback = ({ children }) => <StyledInput.Feedback>{ children }</StyledInput.Feedback>;

export default Feedback;
