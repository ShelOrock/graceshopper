import * as React from 'react';

import * as StyledComponents from '../../StyledComponents/index.jsx';
const { StyledNavigation: { NavLink } } = StyledComponents;

export default () => <NavLink to='/cart'>Shopping Cart</NavLink>;
