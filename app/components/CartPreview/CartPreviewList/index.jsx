import * as React from 'react';

import * as StyledComponents from '../../StyledComponents/index.jsx';
const { StyledList: { LineList } } = StyledComponents;

export default ({ children }) => <LineList>{ children }</LineList>;