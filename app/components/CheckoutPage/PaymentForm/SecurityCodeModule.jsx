import * as React from 'react';

import * as StyledComponents from '../../StyledComponents/index.jsx';
const { StyledStripe: { SecurityCodeInput } } = StyledComponents;

export default () => <SecurityCodeInput />;
