import * as React from 'react';

import * as StyledComponents from '../StyledComponents/index.jsx';
const { StyledCheckoutPage: { CheckoutPageContainer } } = StyledComponents;

export default ({ children }) => <CheckoutPageContainer>{ children }</CheckoutPageContainer>;
