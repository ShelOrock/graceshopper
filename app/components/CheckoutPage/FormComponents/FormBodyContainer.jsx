import * as React from 'react';

import * as StyledComponents from '../../StyledComponents';
const { StyledCheckoutPage: { CheckoutBodyContainer } } = StyledComponents;

export default ({ children }) => <CheckoutBodyContainer>{ children }</CheckoutBodyContainer>;
