import * as React from 'react';

import * as StyledComponents from '../../../StyledComponents/index.jsx';
const { StyledForm: { InputGroup } } = StyledComponents;

export default ({ children }) => <InputGroup>{ children }</InputGroup>;
