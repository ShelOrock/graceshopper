import React from 'react';

import { StyledProduct } from '../../StyledComponents';

const Header = ({ children }) => <StyledProduct.Header>{ children }</StyledProduct.Header>;

export default Header;
