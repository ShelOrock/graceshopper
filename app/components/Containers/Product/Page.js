import * as React from 'react';

import * as StyledComponents from '../../StyledComponents';
const { StyledProductPage: { StyledProductPage } } = StyledComponents;

export default ({ children }) => <StyledProductPage>{ children }</StyledProductPage>;
