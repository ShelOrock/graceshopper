import * as React from 'react';

import * as StyledComponents from '../../StyledComponents';
const { StyledProductPage: { StyledProduct } } = StyledComponents;

export default ({ children }) => <StyledProduct>{ children }</StyledProduct>;
