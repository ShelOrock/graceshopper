import * as React from 'react';

import * as StyledComponents from '../../StyledComponents';
const { StyledShopPage: { StyledShopPage } } = StyledComponents;

export default ({ children }) => <StyledShopPage>{ children }</StyledShopPage>;
