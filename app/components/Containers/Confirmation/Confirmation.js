import React from 'react';

import * as StyledComponents from '../../StyledComponents';
const { StyledConfirmationPage: { StyledConfirmation } } = StyledComponents;

export default ({ children }) => <StyledConfirmation>{ children }</StyledConfirmation>;
