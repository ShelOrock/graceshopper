import React from 'react';

import * as StyledComponents from '../../StyledComponents';
const { StyledConfirmationPage: { StyledButtons } } = StyledComponents;

export default ({ children }) => <StyledButtons>{ children }</StyledButtons>;
