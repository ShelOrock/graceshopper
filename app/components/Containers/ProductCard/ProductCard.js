import * as React from 'react';

import * as StyledComponents from '../../StyledComponents';
const { StyledProductCard: { StyledProductCard } } = StyledComponents;

export default ({ children }) => <StyledProductCard>{ children }</StyledProductCard>;
