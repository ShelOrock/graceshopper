import React from 'react';

import { StyledProductCard } from '../../StyledComponents';

const Media = ({ children }) => <StyledProductCard.Media>{ children }</StyledProductCard.Media>;

export default Media;
