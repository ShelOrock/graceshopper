import * as React from 'react';

import * as StyledComponents from '../../StyledComponents';
const { StyledForm: { StyledInputModule } } = StyledComponents;

export default ({ children }) => <StyledInputModule>{ children }</StyledInputModule>;
