import * as React from 'react';

import * as StyledComponents from '../../StyledComponents';
const { StyledForm: { StyledModules } } = StyledComponents;

export default ({ children }) => <StyledModules>{ children }</StyledModules>;
