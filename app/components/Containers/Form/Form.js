import * as React from 'react';

import * as StyledComponents from '../../StyledComponents';
const { StyledForm: { StyledForm } } = StyledComponents;

export default ({ children }) => <StyledForm>{ children }</StyledForm>;
