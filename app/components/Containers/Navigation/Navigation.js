import * as React from 'react';

import * as StyledComponents from '../../StyledComponents';
const { StyledNavigation: { StyledNavigation } } = StyledComponents;

export default ({ children }) => <StyledNavigation>{ children }</StyledNavigation>;
