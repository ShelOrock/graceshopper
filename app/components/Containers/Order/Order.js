import * as React from 'react';

import * as StyledComponents from '../../StyledComponents';
const { StyledOrderPage: { StyledOrder } } = StyledComponents;

export default ({ children }) => <StyledOrder>{ children }</StyledOrder>;
