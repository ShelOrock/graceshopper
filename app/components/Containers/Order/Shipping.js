import * as React from 'react';

import * as StyledComponents from '../../StyledComponents';
const { StyledOrderPage: { StyledShipping } } = StyledComponents;

export default ({ children }) => <StyledShipping>{ children }</StyledShipping>;
