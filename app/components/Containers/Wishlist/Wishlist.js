import * as React from 'react';

import * as StyledComponents from '../../StyledComponents';
const { StyledWishlistPage: { StyledWishlist } } = StyledComponents;

export default ({ children }) => <StyledWishlist>{ children }</StyledWishlist>;
