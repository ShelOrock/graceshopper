import * as React from 'react';

import * as StyledComponents from '../StyledComponents';
const { StyledPageSelect: { StyledPageSelect } } = StyledComponents;

export default ({ children }) => <StyledPageSelect>{ children }</StyledPageSelect>;
