import React from 'react';

import * as StyledComponents from '../../StyledComponents';
const { StyledWishlistPage: { StyledHeaderButton } } = StyledComponents;

export default ({ children }) => <StyledHeaderButton>{ children }</StyledHeaderButton>;
