import React from 'react';

import * as StyledComponents from '../../StyledComponents';
const { StyledWishlistPage: { StyledBodyButton } } = StyledComponents;

export default ({ children }) => <StyledBodyButton>{ children }</StyledBodyButton>;
