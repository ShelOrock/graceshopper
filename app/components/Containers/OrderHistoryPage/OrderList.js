import * as React from 'react';

import * as StyledComponents from '../../StyledComponents';
const { StyledOrderHistoryPage: { StyledOrderList } } = StyledComponents;

export default ({ children }) => <StyledOrderList>{ children }</StyledOrderList>;
