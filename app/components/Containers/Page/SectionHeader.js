import * as React from 'react';

import * as StyledComponents from '../../StyledComponents';
const { StyledPage: { StyledSectionHeader } } = StyledComponents;

export default ({ children }) => <StyledSectionHeader>{ children }</StyledSectionHeader>;
