import * as React from 'react';

import * as StyledComponents from '../../StyledComponents';
const { StyledPage: { StyledPage } } = StyledComponents;

export default ({ children }) => <StyledPage>{ children }</StyledPage>;
