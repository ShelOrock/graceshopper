import React from 'react';

import { StyledCartCard } from '../../StyledComponents';

const Header = ({ children }) => <StyledCartCard.Header>{ children }</StyledCartCard.Header>;

export default Header;
