import React from 'react';

import { StyledCartCard } from '../../StyledComponents';

const Content = ({ children }) => <StyledCartCard.Content>{ children }</StyledCartCard.Content>;

export default Content;
