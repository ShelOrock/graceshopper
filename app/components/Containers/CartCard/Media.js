import React from 'react';

import { StyledCartCard } from '../../StyledComponents';

const Media = ({ children }) => <StyledCartCard.Media>{ children }</StyledCartCard.Media>;

export default Media;
