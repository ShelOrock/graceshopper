import * as React from 'react';

import * as StyledComponents from '../../StyledComponents';
const { StyledNotFoundPage: { StyledNotFound } } = StyledComponents;

export default ({ children }) => <StyledNotFound>{ children }</StyledNotFound>;
