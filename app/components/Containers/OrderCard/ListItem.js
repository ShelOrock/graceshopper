import * as React from 'react';

import * as StyledComponents from '../../StyledComponents';
const { StyledOrderCard: { StyledListItem } } = StyledComponents;

export default ({ children }) => <StyledListItem>{ children }</StyledListItem>;
