import React from 'react';

import { StyledOrderCard } from '../../StyledComponents';

const Media = ({ children }) => <StyledOrderCard.Media>{ children }</StyledOrderCard.Media>;

export default Media;
