import React from 'react';

import { StyledOrderCard } from '../../StyledComponents';

const Header = ({ children }) => <StyledOrderCard.Header>{ children }</StyledOrderCard.Header>;

export default Header;
