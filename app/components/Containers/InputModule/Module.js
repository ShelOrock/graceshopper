import React from 'react';

import { StyledInputModule } from '../../StyledComponents';

const Module = ({ children }) => <StyledInputModule.Module>{ children }</StyledInputModule.Module>;

export default Module;
