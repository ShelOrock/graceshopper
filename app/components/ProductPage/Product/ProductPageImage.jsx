import * as React from 'react';

import * as StyledComponents from '../../StyledComponents/index.jsx';
const { StyledProductPage: { ProductImage } } = StyledComponents;

export default ({ product }) => (
  <ProductImage src='' alt='Insert Pic Here'/>
)