import * as React from 'react';

import * as StyledComponents from '../../StyledComponents';
const { StyledNavigation: { NavLink } } = StyledComponents;

export default () => <NavLink to='/cart'>Cart</NavLink>;