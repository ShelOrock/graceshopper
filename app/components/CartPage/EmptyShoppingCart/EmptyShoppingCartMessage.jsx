import React from 'react';

import * as StyledComponents from '../../StyledComponents/index.jsx';
const { StyledType: { Body } } = StyledComponents

export default () => <Body>Your shopping cart is empty. Better fix that!</Body>