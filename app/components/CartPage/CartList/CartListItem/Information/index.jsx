import * as React from 'react';

import * as StyledComponents from '../../../../StyledComponents/index.jsx';
const { StyledCartPage: { InfoQuantityContainer } } = StyledComponents;

export default ({ children }) => <InfoQuantityContainer>{ children }</InfoQuantityContainer>;
