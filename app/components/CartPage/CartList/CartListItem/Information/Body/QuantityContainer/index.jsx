import * as React from 'react';

import * as StyledComponents from '../../../../../../StyledComponents/index.jsx';
const { StyledCartPage: { QuantityButtonsContainer } } = StyledComponents;

export default ({ children }) => <QuantityButtonsContainer>{ children }</QuantityButtonsContainer>;