import * as React from 'react';

import * as StyledComponents from '../../../../../StyledComponents/index.jsx';
const { StyledCartPage: { QuantityTotalContainer } } = StyledComponents;

export default ({ children }) => <QuantityTotalContainer>{ children }</QuantityTotalContainer>;
